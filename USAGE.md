# prompt_voice 起動方法と使い方

Windows向けローカル音声入力ツールです。マイクで話すと文字起こしし、指定したモードやプロンプトに沿って整形して表示・クリップボードへコピーします。

---

## 起動方法

### 1. デスクトップで起動（GUI）— 推奨

**いちばん簡単な使い方です。** ウィンドウが開き、「話す」ボタンで録音開始・「停止」で確定・コピーまで一通り操作できます。

1. **ショートカットを作る**
   - エクスプローラーで `prompt_voice` フォルダを開く
   - **`起動.bat`** または **`run_gui.bat`** を **右クリック** → **送る** → **デスクトップ（ショートカットを作成）**
   - 必要ならショートカットの名前を「prompt_voice」などに変更すると分かりやすいです

2. **起動**
   - デスクトップのショートカットを **ダブルクリック**
   - 最初だけ黒い窓が一瞬出ることがありますが、すぐに **prompt_voice のウィンドウ** が開きます
   - 終了するときはウィンドウの × で閉じるだけです

**注意**
- Python がインストールされ、`python` で起動できること（PATH に通っていること）が必要です
- バッチは `prompt_voice` フォルダ内に置いたまま、ショートカットだけデスクトップに置いてください（フォルダを移すとパスがずれます）

### 2. CLI（main.py）

プロジェクトルート（`Realice`）で:

```powershell
python prompt_voice\main.py
```

オプション付きの例:

```powershell
python prompt_voice\main.py --mode clean --show-partial
python prompt_voice\main.py --prompt "箇条書きで整理して"
python prompt_voice\main.py --list-devices
```

### 3. TUI（Textual）

```powershell
cd prompt_voice
python tui_main.py
```

または、`prompt_voice` がパスに含まれる状態で:

```powershell
python -m prompt_voice.tui_main
```

※ デスクトップから TUI で起動したい場合は、`run_tui.bat` のショートカットをデスクトップに作ればダブルクリックで起動できます。

---

## 使い方（GUI）

1. **「🎙 話す」** をクリック → 録音開始（ボタンが「⏹ 停止」に変わり赤くなります）
2. **そのまま話す / 会議音を流す** → 話し中は「話し中（暫定）」にリアルタイム表示（オプションでオフ可）
3. **「⏹ 停止」** をクリック → 発話が確定し、整形されたテキストが「確定テキスト」に追加され、クリップボードにコピーされます
4. 必要なら **「📋 クリップボードにコピー」** で再コピー、**「クリア」** で表示を消せます

オプションで「出力」（raw / clean / cursor / minutes）、「確定時にコピー」「話し中も表示」などを変更できます。入力デバイスは **自動でマイク + 内部音（既定スピーカー）** を利用します。プロンプト欄に固有名詞や整形のヒントを書くと認識・整形の精度が上がります。

---

## 使い方（CLI）

### 基本の流れ

1. **起動** → 入力診断（約2.5秒）が走り、「入力OK」または「音が入っていません」が表示される
2. **プロンプト入力**（未指定時のみ）  
   「整形プロンプトを入力してください（空でスキップ）。複数行OK、空行で確定。」と出たら、指示を入力して空行でEnter
3. **話す** → 発話が途切れると自動で確定し、整形結果が表示されクリップボードにコピーされる
4. **終了** → `Ctrl+C`

### 主なオプション

| オプション | 説明 |
|-----------|------|
| `--mode {raw,clean,cursor,minutes}` | 出力の整形モード（既定: clean） |
| `--prompt "..."` | 整形用プロンプト（省略時は起動後に対話入力） |
| `--device 番号または名前` | マイク入力の上書き指定（通常は不要。既定では自動入力） |
| `--show-partial` | 話している最中に暫定結果を表示 |
| `--no-copy` | 確定時にクリップボードへコピーしない |
| `--auto-paste` | コピー後に Ctrl+V を送信 |
| `--push-to-talk` | Ctrl+Space を押している間だけ録音 |
| `--list-devices` | 利用可能な入力デバイス一覧を表示して終了 |
| `--log-level DEBUG` | ログレベル |

### プロンプトの例

- 空のまま（Enterのみ）→ モードのみで整形（従来どおり）
- `箇条書きで` → 文を箇条書きに整理
- `議事録風に` → 決定事項・課題・ネクストアクション風に分類
- `{text}` を含む文字列 → そのテンプレートに認識結果を埋め込む

---

## 使い方（TUI）

1. **起動** → サイドバーにステータス・モード・ショートカットが表示される
2. **R** → 録音開始／停止
3. **M** → モード切替（raw / clean / cursor / minutes）
4. **C** → 最後の確定結果をクリップボードにコピー
5. **Q** → 終了

※ TUI では現状、プロンプトは `RecordingSession` の `initial_prompt` 経由で渡す設計だが、起動時に対話でプロンプトを聞くUIは未実装。CLIの `--prompt` 相当を使う場合は、コード側で `RecordingSession(..., initial_prompt="...")` を渡す必要あり。

---

## トラブル時

- **音が入っていません**  
  Windowsのマイク権限（設定 → プライバシー → マイク）、既定の入力デバイス、ミュートを確認。会議相手の声はスピーカー出力のため、マイクだけでは拾えません。
- **内部音が拾えない**  
  Windows の音量ミキサーで対象アプリの出力先が「既定スピーカー」になっているか確認してください。環境によってはループバック非対応で、マイクのみになる場合があります。
- **文字化け**  
  PowerShell の文字コードを UTF-8 に: `chcp 65001` のあと起動。

---

## 履歴

確定結果は `--history-path` で指定したファイル（既定: `logs/history.json`）に追記されます。
